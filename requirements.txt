
# Fallback manuel (local uniquement, cf. README) : `playwright install
# --with-deps chromium` après ce pip install.
playwright>=1.45
python-dotenv>=1.0
pydantic>=2.7
PyYAML>=6.0
httpx>=0.27
beautifulsoup4>=4.12

# dev
pytest>=8.0
